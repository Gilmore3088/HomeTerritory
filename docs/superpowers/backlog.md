# Territory backlog

Deferred observations, tagged by future phase (P2 UX/visual, P3 trivia,
P4 strategy/meta, P5 go-live).

| Phase | Item | Source |
| --- | --- | --- |
| P1-audit | `game_submit_answer` (20260730082200_handoff_answer_resolution.sql) special-cases `action_type = 'home'` to always return `status: 'completed'`, win or lose — a wrong answer only drops the resulting `hold_level` to 1 instead of 2, it never fails the session. Task 6's brief drafted its "wrong answer fails a session" test against a `home` action on that assumption; real behavior meant that test would have passed trivially without exercising failure at all. Confirmed intentional design (home ground can't be lost), not a bug — adapted the test to use a `claim` action instead, which does route through the real success/fail branch. | tests/db/engine.test.ts (task 6) |
| P2 | The question timer shows only seconds (`0:07`) with no visual countdown, and the auto-submit on expiry fires `submit("")` which renders the generic "Incorrect. The map does not change." — a player who ran out of time cannot tell that from a wrong answer. Give the timeout its own result copy. | task 7 audit, now `components/question-arena.tsx` |
| P2 | `TerritorySheet` disables the action button but never explains why. At zero actions the claim/attack button is greyed with no message, and the sheet's explanatory line only covers adjacency and contested state. Surface the actual blocker (no actions / cooling down / already fortified today). | task 7 audit, now `TerritorySheet` in `components/game-shell.tsx` |
| P2 | `loadSnapshot` sets the active operation but never clears it: `if (operationResponse.data) setOperation(...)` has no `else setOperation(null)`. A session resolved in another tab leaves a stale question card until a full reload. | task 7 audit, now `loadSnapshot` in `hooks/use-game-state.ts` |
| P2 | Two independent components (`TerritoryGame`, `GameRuntimeControls`) each mount on `/` and each run their own `get_my_groups` + `group_snapshot` + `get_my_active_session` loop (20s and 5s respectively), reading the same `territory_group` localStorage key. Triple the RPC traffic for one screen, and they can disagree about which league is selected. Merge into one data source. | task 7 audit, `app/page.tsx` |
| P2 | `components/pwa-register.tsx` is imported nowhere and `app/layout.tsx` emits no `<link rel="manifest">`, so `public/sw.js` and `public/manifest.webmanifest` ship but never load. Either wire the PWA up or delete the three assets and the README claim. | task 7 audit (finding 18e) |
| P2 | `report_question` in `GameRuntimeControls` uses `window.prompt` / `window.alert` and then `window.location.reload()`. Works, but it is the only native-dialog surface in an otherwise custom UI. | task 7 audit, `components/game-runtime-controls.tsx:164-186` |
| P3 | The 550-question starter bank is 11 rows per state generated from a single subject per state, so all 11 answers for a state are the same string in different question phrasings. Any player who sees one question for a state knows every answer for that state for the rest of the season. This is the single biggest gameplay quality gap and it is by design ("starter_seed"); replacing it is the trivia phase's core job. | task 7 audit, `supabase/migrations/202607300001_initial_schema.sql:968-1011` |
| P3 | `answer_matches` accepts any free-fill answer within Levenshtein distance 2 once the normalized answer exceeds 6 characters. Combined with the single-subject bank above, near-misses on short team names are inconsistent — "Oregon Duck" passes, "Ducks" does not. Revisit the fuzzy threshold with a real bank. | task 7 audit, `supabase/migrations/202607300001_initial_schema.sql:244` |
| P3 | Question reporting needs a review queue and a threshold rather than the immediate global deactivation of finding 9. Also needs a way to *reactivate* a question — nothing in the schema or UI can undo `active = false`. | task 7 audit (finding 9) |
| P4 | The region bonus in `run_daily_tick` compares a player's owned states in a region against `count(*) from public.territories` for that region, not against the states actually in the season. On a `lower48` board the Pacific region can never be completed (AK and HI are excluded from `season_territories` but still counted), so that bonus is unreachable. Latent today because the UI hardcodes `board_scope: 'fifty'`. | task 7 audit, `supabase/migrations/20260802173100_complete_stabilization_functions.sql:212-220` |
| P4 | `create_group_v2` accepts `opening_mode`, `board_scope`, `difficulty` and season lengths of 7/10/14/30/60, and the engine honours all of them (difficulty shifts attack cost by ±1, `dealt` pre-assigns the whole map). The UI hardcodes `open` / `fifty` / `standard` / 14 and exposes none of it. Either surface the options or drop the dead parameters. | task 7 audit, now `components/league-entry.tsx` |
| P4 | Twilight decay (`run_daily_tick` strips one garrison level from every uncontested state in the last three days of a season) has no representation anywhere in the UI — no warning before it starts, no activity-feed styling, no explanation of why every state suddenly weakened. | task 7 audit, `supabase/migrations/20260802173100_complete_stabilization_functions.sql:192-199` |
| P4 | `pick_next_question`'s exhaustion fallback ignores both the 7-day re-serve window and the session tier, so a depleted state can re-serve the question a player just answered inside the same attack streak. Harmless with a full bank; see finding 20. | task 7 audit (finding 20) |
| P5 | ~~`20260730074000_support_test_bot_players.sql` drops `profiles_id_fkey`, so `profiles.id` no longer references `auth.users` and deleting an auth user leaves an orphaned profile plus every row that references it.~~ **Done in `20260804210000_remove_bot_players.sql`.** Bots were removed from the game outright (they were playtest scaffolding; a trivia game has no honest bot opponent), bot/orphaned rows were cleaned, and the FK was restored with `on delete cascade` — deleting an auth user now cascades to its profile. | task 7 audit, closed by bot removal |
| P5 | `supabase/config.toml` points `db.seed.sql_paths` at a nonexistent `supabase/seed.sql` (CLI default). Confirmed intentional — the 550 questions are inserted by the initial migration, not a seed file — but `supabase db reset` emits a WARN every run. Either drop the seed path from config or add an empty seed file. | task 5 handoff, confirmed task 7 |
| P5 | Two-device production realtime check is still outstanding. The local stack publishes all four subscribed tables correctly, but RLS-filtered Postgres Changes under a real project's connection limits is a different regime. Flagged for the two-phone pass. | task 7 audit |
| P1-audit | `npx eslint` crashes repo-wide (`TypeError: Error while loading rule 'react/display-name': contextOrFilename.getFilename is not a function`) on any file, including pre-existing ones (e.g. `lib/game-format.ts`) unrelated to this task's changes. Looks like an eslint-plugin-react / ESLint 10 / Next 16 version-detection incompatibility. Not caused by Task 6's new `tests/db/` files; `npm test`, `npm run typecheck`, and `npm run build` (the task's actual gates) all pass. Flagging for a future task since `npm run lint` is currently unusable. | tests/db/ verification pass (task 6) |
| P2-day-counter | Season-level game-day counter so the commissioner can fast-forward multiple days independent of the wall calendar — deferred from P2a; advance_group_day currently settles the current local day only. | P2a foundation closeout (task 8), `20260804000000_commissioner_advance_group_day.sql` |

## Deferred from Task 8 (fix audit findings)

| Item | Why deferred | Tag |
|---|---|---|
| ~~Delete the seven dead API routes under `app/api/**`~~ **Done in task 9 (8bf8aae).** Re-verified first: `grep -rn "/api/" app components lib hooks tests public proxy.ts next.config.ts vercel.json` outside `app/api/` matched only `vercel.json`'s cron entry, and there is no `fetch(` anywhere in the client code. All seven removed; `app/api/cron/tick` kept. Finding 13. | Was: Task 9's cleanup scope, landing with the finding 18 README corrections. | P1-task9 — closed |
| ~~Correct the five stale README claims~~ **Done in task 9 (5bc9741).** All five fixed, plus three more the pass turned up: the map is not zoomable, quarantine now needs three reports rather than one, and "Run locally" said nothing about the local stack. Finding 18. | Was: same change set as finding 13. | P1-task9 — closed |
| Make `groups.timezone` settable: `create_group_v2` takes no timezone parameter and no UI or RPC writes it, so every league scores on Pacific time. Finding 12. | Papercut, and the fix is a new RPC parameter plus a create-league form field plus a per-group cron consideration (`vercel.json` runs a single `5 8 * * *` UTC schedule) — well past the ~10-line papercut budget. `public.group_local_date()` from finding 11 already reads the column correctly, so only the writer is missing. | P2-timezone |
| Tighten `Access-Control-Allow-Origin: *` on the `test-signup` edge function. Noted in finding 14. | Needs an allowed-origin configuration story (env var plus preview-deployment origins), which is deployment work rather than a code fix. The takeover path itself is fixed. | P2-signup-cors |
| Split `tests/db/audit.test.ts` (941 lines, over the 300-line house limit). | The bank-mutating probes (findings 1 and 9 both park a territory's whole question bank) rely on node:test running a file's tests sequentially. Splitting makes them race across parallel test *files*, so it needs per-territory isolation first. | P2-test-split |
| ~~Clear the 3 `react-hooks/set-state-in-effect` errors and 1 `react-hooks/exhaustive-deps` warning~~ **Done in task 9 (89f13ed),** after the split moved the effects into `hooks/use-game-state.ts` and `components/question-arena.tsx`. No rule was disabled: the sign-out wipe became a guarded render-phase transition, each server read starts from a local async function so its writes land in the RPC continuation, the answer box clears in the render that first shows a new attempt, and the countdown reaches the latest `submit` through a ref. `npm run lint` exits 0. | Was: the same effects Task 9 was about to move. | P1-task9 — closed |

## Deferred from the final whole-branch review

| Item | Why deferred | Tag |
|---|---|---|
| Wire `test:db` + `test:smoke` into CI with a Supabase stack service. `.github/workflows/ci.yml` currently runs only `npm test`, `npm run typecheck`, and `npm run build` on every push/PR; the DB-engine suite, the smoke test, and the security-definer grant audits (anon/authenticated allowlists, answer-leak probe) all run locally only and do not gate `main`. Needs a `services:`-style Postgres/Supabase container (or an equivalent `supabase start` step) in the workflow, plus the `SUPABASE_TEST_*` env vars documented in `docs/superpowers/local-stack.md`. | Real CI infrastructure work (service container, migration bootstrap, secrets), not a test-content change — out of scope for the fix wave that found the gap. | P1-followup |
| De-flake the DB suite: add `auth.users` teardown between tests, cap `node:test` concurrency, and address `run_daily_tick`'s O(all-seasons) growth (finding 10 fixed the *correctness* bug -- the tick no longer runs on every snapshot -- but the function itself still scans every active season on each real invocation, so the suite's cost grows with the DB's total accumulated test users/seasons over time). Symptom seen in practice: the documented `AuthRetryableFetchError` GoTrue flake that needs a `docker restart supabase_kong_HomeTerritory` retry. | Needs a shared per-file (or per-suite) teardown hook and a concurrency audit across `tests/db/*.test.ts`, which touches test infrastructure broadly rather than one probe — scoped as its own follow-up rather than folded into an unrelated fix. | P1-followup |
| `create_group` (v1) is still `grant execute ... to authenticated`, even though `create_group_v2` fully superseded it and no client code calls it any more (finding 13 deleted the v1 API route, but never revoked the underlying RPC's grant). Internally guarded (`auth.uid()` required, its own validation), so not an auth bypass, but it lets any signed-in user create an old-shape league (no `test_mode`/`board_scope`/`opening_mode`) that the current UI cannot drive. `tests/db/audit.test.ts`'s authenticated-grants allowlist (S4 hardening) now names this explicitly rather than hiding it, so it will not regress silently; the actual fix is a `revoke execute on function public.create_group(text, text[], integer) from authenticated;` migration. | Fixing it means writing a new migration, which is SQL/application behavior change and out of scope for a test-hardening fix wave. | P1-security-grant |

## Deferred from P2a final whole-branch review (P2b)
| Phase | Item | Source |
|-------|------|--------|
| P2b | Restore a refresh on window focus / visibilitychange (the P2a data-merge kept only the 20s poll + realtime; tab-refocus is slightly less snappy) | P2a final review |
| P2b | Auto-clear the turn-banner `message` (the old 5s loop reset it; now the "X is up" line lingers until the next end-turn) — fold into the P2b turn-banner restyle | P2a final review |
